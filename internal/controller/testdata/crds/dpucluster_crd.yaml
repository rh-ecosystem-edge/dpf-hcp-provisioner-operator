---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dpuclusters.provisioning.dpu.nvidia.com
spec:
  group: provisioning.dpu.nvidia.com
  names:
    kind: DPUCluster
    listKind: DPUClusterList
    plural: dpuclusters
    singular: dpucluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DPUCluster is a minimal schema for testing purposes
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: DPUCluster spec
            properties:
              type:
                description: Cluster type (e.g., bf3, static)
                type: string
              maxNodes:
                description: Maximum number of nodes in the cluster
                type: integer
              kubeconfig:
                description: Secret name containing admin kubeconfig
                type: string
            type: object
          status:
            description: DPUCluster status
            properties:
              conditions:
                description: Status conditions
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              phase:
                description: Cluster phase (e.g., Ready, Pending, Failed)
                type: string
              version:
                description: Cluster version
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
